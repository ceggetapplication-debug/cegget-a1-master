import React from 'react';
import { View, Text, StyleSheet, Modal, TouchableOpacity, ScrollView } from 'react-native';
import Ionicons from 'react-native-vector-icons/Ionicons';

interface AvantagesModalProps {
    visible: boolean;
    onClose: () => void;
    creditAmount: number;
    hasPremium: boolean;
    numberOfFreeDeliveries: number;
    onPremiumPress: () => void;
    onInvitePress: () => void;
}

export const AvantagesModal: React.FC<AvantagesModalProps> = ({
    visible,
    onClose,
    creditAmount,
    hasPremium,
    numberOfFreeDeliveries,
    onPremiumPress,
    onInvitePress
}) => {
    return (
        <Modal
            animationType="slide"
            transparent={true}
            visible={visible}
            onRequestClose={onClose}
        >
            <View style={styles.centeredView}>
                <View style={styles.modalView}>
                    {/* HEADER */}
                    <View style={styles.header}>
                        <TouchableOpacity onPress={onClose} style={styles.backButton}>
                            <Ionicons name="arrow-back" size={28} color="#333" />
                        </TouchableOpacity>
                        <Text style={styles.title}>Vos avantages</Text>
                    </View>

                    <ScrollView style={styles.scrollView} contentContainerStyle={styles.scrollContent}>
                        {/* CRÉDIT CUMULÉ */}
                        {hasPremium ? (
                            <View style={styles.section}>
                                <Text style={styles.text}>
                                    Votre crédit cumulé est de : <Text style={styles.amount}>{creditAmount.toFixed(2)} DZD</Text>
                                </Text>
                            </View>
                        ) : (
                            <View style={styles.section}>
                                <Text style={styles.text}>Vous voulez du crédit cumulé ??</Text>
                                <TouchableOpacity style={styles.button} onPress={onPremiumPress}>
                                    <Text style={styles.buttonText}>Premium</Text>
                                </TouchableOpacity>
                            </View>
                        )}

                        <View style={styles.section}>
                            <Text style={styles.text}>
                                Vous avez {numberOfFreeDeliveries} livraisons gratuites. Voulez-vous en avoir d'avantages ??
                            </Text>
                            <TouchableOpacity style={[styles.button, styles.inviteButton]} onPress={onInvitePress}>
                                <Text style={styles.buttonText}>Inviter</Text>
                            </TouchableOpacity>
                        </View>
                    </ScrollView>
                </View>
            </View>
        </Modal>
    );
};

const styles = StyleSheet.create({
    centeredView: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor: 'rgba(0,0,0,0.5)',
    },
    modalView: {
        width: '90%',
        maxHeight: '80%',
        backgroundColor: 'white',
        borderRadius: 20,
        padding: 20,
        shadowColor: '#000',
        shadowOffset: { width: 0, height: 2 },
        shadowOpacity: 0.25,
        shadowRadius: 4,
        elevation: 5,
    },
    header: {
        flexDirection: 'row',
        alignItems: 'center',
        marginBottom: 25,
        position: 'relative',
    },
    backButton: {
        position: 'absolute',
        left: 0,
        padding: 5,
    },
    title: {
        fontSize: 24,
        fontWeight: 'bold',
        color: '#333',
        textAlign: 'center',
        flex: 1,
    },
    scrollView: {
        width: '100%',
    },
    scrollContent: {
        paddingBottom: 20,
    },
    section: {
        marginBottom: 30,
    },
    text: {
        fontSize: 18,
        color: '#555',
        marginBottom: 15,
        textAlign: 'center',
        lineHeight: 26,
    },
    amount: {
        fontSize: 20,
        fontWeight: 'bold',
        color: '#28a745',
    },
    button: {
        backgroundColor: '#007bff',
        paddingVertical: 15,
        paddingHorizontal: 30,
        borderRadius: 10,
        alignItems: 'center',
        shadowColor: '#000',
        shadowOffset: { width: 0, height: 2 },
        shadowOpacity: 0.2,
        shadowRadius: 3,
        elevation: 4,
    },
    inviteButton: {
        backgroundColor: '#28a745',
    },
    buttonText: {
        color: '#fff',
        fontSize: 18,
        fontWeight: 'bold',
    },
});

